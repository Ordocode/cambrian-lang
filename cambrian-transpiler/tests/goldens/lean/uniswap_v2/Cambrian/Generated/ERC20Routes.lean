/-
  Auto-generated by cambrian-transpiler — Lean target (P3).
  Routes for `ERC20` — `<E>.Local.*` state transitions + World wrappers.
-/

import Cambrian.Prelude
import Cambrian.Generated.ERC20
import Cambrian.Generated.World
import Cambrian.Generated.Dispatch

set_option linter.unusedVariables false

namespace ERC20.Local

def constructor (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (initial_holder : Cambrian.Address) (initial_supply : Nat) : ERC20.State :=
  let s := { s with m_total_supply := ERC20.Members.M_m_total_supply.constructor s ctx inst initial_holder initial_supply, m_balances := ERC20.Members.M_m_balances.constructor s ctx inst initial_holder initial_supply }
  s

attribute [cambrian_route_simp] constructor

def transfer_pre_0 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (to : Cambrian.Address) (amount : Nat) : Bool :=
  (amount > 0)

def transfer_pre_1 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (to : Cambrian.Address) (amount : Nat) : Bool :=
  ((Cambrian.Generated.Pure.balance_of s.m_balances ctx.sender) ≥ amount)

def transfer (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (to : Cambrian.Address) (amount : Nat) : Cambrian.RouteResult (ERC20.State) :=
  do
    if !(transfer_pre_0 s ctx inst to amount) then throw (Cambrian.ThrowCode.ofNat 1)
    if !(transfer_pre_1 s ctx inst to amount) then throw (Cambrian.ThrowCode.ofNat 2)
    let s := { s with m_balances := ERC20.Members.M_m_balances.transfer s ctx inst to amount }
    pure s


attribute [cambrian_route_simp] transfer
attribute [cambrian_pre_simp] transfer_pre_0 transfer_pre_1

def transferFrom_pre_0 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) : Bool :=
  (amount > 0)

def transferFrom_pre_1 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) : Bool :=
  ((Cambrian.Generated.Pure.balance_of s.m_balances from_addr) ≥ amount)

def transferFrom_pre_2 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) : Bool :=
  ((Cambrian.Generated.Pure.allowance_of s.m_allowances from_addr ctx.sender) ≥ amount)

def transferFrom (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) : Cambrian.RouteResult (ERC20.State) :=
  do
    if !(transferFrom_pre_0 s ctx inst from_addr to amount) then throw (Cambrian.ThrowCode.ofNat 1)
    if !(transferFrom_pre_1 s ctx inst from_addr to amount) then throw (Cambrian.ThrowCode.ofNat 2)
    if !(transferFrom_pre_2 s ctx inst from_addr to amount) then throw (Cambrian.ThrowCode.ofNat 3)
    let s := { s with m_balances := ERC20.Members.M_m_balances.transferFrom s ctx inst from_addr to amount, m_allowances := ERC20.Members.M_m_allowances.transferFrom s ctx inst from_addr to amount }
    pure s


attribute [cambrian_route_simp] transferFrom
attribute [cambrian_pre_simp] transferFrom_pre_0 transferFrom_pre_1 transferFrom_pre_2

def approve (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (spender : Cambrian.Address) (amount : Nat) : ERC20.State :=
  let s := { s with m_allowances := ERC20.Members.M_m_allowances.approve s ctx inst spender amount }
  s

attribute [cambrian_route_simp] approve

def permit_pre_0 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (owner : Cambrian.Address) (spender : Cambrian.Address) (value : Nat) (deadline : Nat) (v : Nat) (r : Nat) (s_p : Nat) : Bool :=
  (deadline ≥ (ctx.timestamp).toNat)

def permit_pre_1 (w : Cambrian.Generated.World) (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (owner : Cambrian.Address) (spender : Cambrian.Address) (value : Nat) (deadline : Nat) (v : Nat) (r : Nat) (s_p : Nat) : Bool :=
  ((Cambrian.Generated.Pure.permit_signer owner spender value (Cambrian.Generated.Pure.nonce_of s.m_nonces owner) deadline (((w.block.chainId).zeroExtend 256)).toNat (ERC20.address inst) v r s_p) == owner)

def permit (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (owner : Cambrian.Address) (spender : Cambrian.Address) (value : Nat) (deadline : Nat) (v : Nat) (r : Nat) (s_p : Nat) : Cambrian.RouteResult (ERC20.State) :=
  do
    if !(permit_pre_0 s ctx inst owner spender value deadline v r s_p) then throw (Cambrian.ThrowCode.ofNat 4)
    let s := { s with m_nonces := ERC20.Members.M_m_nonces.permit s ctx inst owner spender value deadline v r s_p, m_allowances := ERC20.Members.M_m_allowances.permit s ctx inst owner spender value deadline v r s_p }
    pure s


attribute [cambrian_route_simp] permit
attribute [cambrian_pre_simp] permit_pre_0 permit_pre_1

def mint_pre_0 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (to : Cambrian.Address) (amount : Nat) : Bool :=
  (amount > 0)

def mint (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (to : Cambrian.Address) (amount : Nat) : Cambrian.RouteResult (ERC20.State) :=
  do
    if !(mint_pre_0 s ctx inst to amount) then throw (Cambrian.ThrowCode.ofNat 1)
    let s := { s with m_total_supply := ERC20.Members.M_m_total_supply.mint s ctx inst to amount, m_balances := ERC20.Members.M_m_balances.mint s ctx inst to amount }
    pure s


attribute [cambrian_route_simp] mint
attribute [cambrian_pre_simp] mint_pre_0

def burn_pre_0 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (amount : Nat) : Bool :=
  (amount > 0)

def burn_pre_1 (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (amount : Nat) : Bool :=
  ((Cambrian.Generated.Pure.balance_of s.m_balances ctx.sender) ≥ amount)

def burn (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (amount : Nat) : Cambrian.RouteResult (ERC20.State) :=
  do
    if !(burn_pre_0 s ctx inst amount) then throw (Cambrian.ThrowCode.ofNat 1)
    if !(burn_pre_1 s ctx inst amount) then throw (Cambrian.ThrowCode.ofNat 2)
    let s := { s with m_total_supply := ERC20.Members.M_m_total_supply.burn s ctx inst amount, m_balances := ERC20.Members.M_m_balances.burn s ctx inst amount }
    pure s


attribute [cambrian_route_simp] burn
attribute [cambrian_pre_simp] burn_pre_0 burn_pre_1

def balanceOf (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (owner : Cambrian.Address) : ERC20.State × Nat :=
  (s, (Cambrian.Generated.Pure.balance_of s.m_balances owner))

attribute [cambrian_route_simp] balanceOf

def allowance (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (owner : Cambrian.Address) (spender : Cambrian.Address) : ERC20.State × Nat :=
  (s, (Cambrian.Generated.Pure.allowance_of s.m_allowances owner spender))

attribute [cambrian_route_simp] allowance

def totalSupply (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) : ERC20.State × Nat :=
  (s, s.m_total_supply)

attribute [cambrian_route_simp] totalSupply

def tokenId (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) : ERC20.State × Nat :=
  (s, inst.m_token_id)

attribute [cambrian_route_simp] tokenId

def nonces (s : ERC20.State) (ctx : Cambrian.MsgCtx) (inst : ERC20.Identity) (owner : Cambrian.Address) : ERC20.State × Nat :=
  (s, (Cambrian.Generated.Pure.nonce_of s.m_nonces owner))

attribute [cambrian_route_simp] nonces

end ERC20.Local

namespace ERC20.Routes

def constructor (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (initial_holder : Cambrian.Address) (initial_supply : Nat) : Cambrian.Generated.World :=
  let s := w.storage.eRC20 inst
  let s := ERC20.Local.constructor s ctx inst initial_holder initial_supply
  Cambrian.Generated.World.withERC20 w inst s



attribute [cambrian_route_simp] constructor

def transfer (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) : Cambrian.RouteResult (Cambrian.Generated.World) :=
  do
    let s := w.storage.eRC20 inst
    let s ← ERC20.Local.transfer s ctx inst to amount
    pure (Cambrian.Generated.World.withERC20 w inst s)



attribute [cambrian_route_simp] transfer

def transfer.Pre (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) : Prop :=
  let s := w.storage.eRC20 inst
  ERC20.Local.transfer_pre_0 s ctx inst to amount = true ∧ ERC20.Local.transfer_pre_1 s ctx inst to amount = true

instance (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) : Decidable (transfer.Pre w inst ctx to amount) := by
  unfold transfer.Pre; infer_instance

theorem transfer_isOk_iff (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) :
    (transfer w inst ctx to amount).isOk = true ↔ transfer.Pre w inst ctx to amount := by
  unfold transfer transfer.Pre
  simp [cambrian_route_simp, Bool.and_eq_true, Bool.or_eq_true, decide_eq_true_eq]

def transferFrom (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) : Cambrian.RouteResult (Cambrian.Generated.World) :=
  do
    let s := w.storage.eRC20 inst
    let s ← ERC20.Local.transferFrom s ctx inst from_addr to amount
    pure (Cambrian.Generated.World.withERC20 w inst s)



attribute [cambrian_route_simp] transferFrom

def transferFrom.Pre (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) : Prop :=
  let s := w.storage.eRC20 inst
  ERC20.Local.transferFrom_pre_0 s ctx inst from_addr to amount = true ∧ ERC20.Local.transferFrom_pre_1 s ctx inst from_addr to amount = true ∧ ERC20.Local.transferFrom_pre_2 s ctx inst from_addr to amount = true

instance (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) : Decidable (transferFrom.Pre w inst ctx from_addr to amount) := by
  unfold transferFrom.Pre; infer_instance

theorem transferFrom_isOk_iff (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (from_addr : Cambrian.Address) (to : Cambrian.Address) (amount : Nat) :
    (transferFrom w inst ctx from_addr to amount).isOk = true ↔ transferFrom.Pre w inst ctx from_addr to amount := by
  unfold transferFrom transferFrom.Pre
  simp [cambrian_route_simp, Bool.and_eq_true, Bool.or_eq_true, decide_eq_true_eq]

def approve (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (spender : Cambrian.Address) (amount : Nat) : Cambrian.Generated.World :=
  let s := w.storage.eRC20 inst
  let s := ERC20.Local.approve s ctx inst spender amount
  Cambrian.Generated.World.withERC20 w inst s



attribute [cambrian_route_simp] approve

def permit (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (owner : Cambrian.Address) (spender : Cambrian.Address) (value : Nat) (deadline : Nat) (v : Nat) (r : Nat) (s_p : Nat) : Cambrian.RouteResult (Cambrian.Generated.World) :=
  do
    let s := w.storage.eRC20 inst
    if !(ERC20.Local.permit_pre_1 w s ctx inst owner spender value deadline v r s_p) then throw (Cambrian.ThrowCode.ofNat 5)
    let s ← ERC20.Local.permit s ctx inst owner spender value deadline v r s_p
    pure (Cambrian.Generated.World.withERC20 w inst s)



attribute [cambrian_route_simp] permit

def permit.Pre (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (owner : Cambrian.Address) (spender : Cambrian.Address) (value : Nat) (deadline : Nat) (v : Nat) (r : Nat) (s_p : Nat) : Prop :=
  let s := w.storage.eRC20 inst
  ERC20.Local.permit_pre_1 w s ctx inst owner spender value deadline v r s_p = true ∧ ERC20.Local.permit_pre_0 s ctx inst owner spender value deadline v r s_p = true

instance (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (owner : Cambrian.Address) (spender : Cambrian.Address) (value : Nat) (deadline : Nat) (v : Nat) (r : Nat) (s_p : Nat) : Decidable (permit.Pre w inst ctx owner spender value deadline v r s_p) := by
  unfold permit.Pre; infer_instance

theorem permit_isOk_iff (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (owner : Cambrian.Address) (spender : Cambrian.Address) (value : Nat) (deadline : Nat) (v : Nat) (r : Nat) (s_p : Nat) :
    (permit w inst ctx owner spender value deadline v r s_p).isOk = true ↔ permit.Pre w inst ctx owner spender value deadline v r s_p := by
  unfold permit permit.Pre
  simp [cambrian_route_simp, Bool.and_eq_true, Bool.or_eq_true, decide_eq_true_eq]

def mint (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) : Cambrian.RouteResult (Cambrian.Generated.World) :=
  do
    let s := w.storage.eRC20 inst
    let s ← ERC20.Local.mint s ctx inst to amount
    pure (Cambrian.Generated.World.withERC20 w inst s)



attribute [cambrian_route_simp] mint

def mint.Pre (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) : Prop :=
  let s := w.storage.eRC20 inst
  ERC20.Local.mint_pre_0 s ctx inst to amount = true

instance (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) : Decidable (mint.Pre w inst ctx to amount) := by
  unfold mint.Pre; infer_instance

theorem mint_isOk_iff (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (to : Cambrian.Address) (amount : Nat) :
    (mint w inst ctx to amount).isOk = true ↔ mint.Pre w inst ctx to amount := by
  unfold mint mint.Pre
  simp [cambrian_route_simp, Bool.and_eq_true, Bool.or_eq_true, decide_eq_true_eq]

def burn (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (amount : Nat) : Cambrian.RouteResult (Cambrian.Generated.World) :=
  do
    let s := w.storage.eRC20 inst
    let s ← ERC20.Local.burn s ctx inst amount
    pure (Cambrian.Generated.World.withERC20 w inst s)



attribute [cambrian_route_simp] burn

def burn.Pre (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (amount : Nat) : Prop :=
  let s := w.storage.eRC20 inst
  ERC20.Local.burn_pre_0 s ctx inst amount = true ∧ ERC20.Local.burn_pre_1 s ctx inst amount = true

instance (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (amount : Nat) : Decidable (burn.Pre w inst ctx amount) := by
  unfold burn.Pre; infer_instance

theorem burn_isOk_iff (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (amount : Nat) :
    (burn w inst ctx amount).isOk = true ↔ burn.Pre w inst ctx amount := by
  unfold burn burn.Pre
  simp [cambrian_route_simp, Bool.and_eq_true, Bool.or_eq_true, decide_eq_true_eq]

def balanceOf (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (owner : Cambrian.Address) : Cambrian.Generated.World × Nat :=
  let s := w.storage.eRC20 inst
  let (s, payload) := ERC20.Local.balanceOf s ctx inst owner
  (Cambrian.Generated.World.withERC20 w inst s, payload)



attribute [cambrian_route_simp] balanceOf

def allowance (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (owner : Cambrian.Address) (spender : Cambrian.Address) : Cambrian.Generated.World × Nat :=
  let s := w.storage.eRC20 inst
  let (s, payload) := ERC20.Local.allowance s ctx inst owner spender
  (Cambrian.Generated.World.withERC20 w inst s, payload)



attribute [cambrian_route_simp] allowance

def totalSupply (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) : Cambrian.Generated.World × Nat :=
  let s := w.storage.eRC20 inst
  let (s, payload) := ERC20.Local.totalSupply s ctx inst
  (Cambrian.Generated.World.withERC20 w inst s, payload)



attribute [cambrian_route_simp] totalSupply

def tokenId (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) : Cambrian.Generated.World × Nat :=
  let s := w.storage.eRC20 inst
  let (s, payload) := ERC20.Local.tokenId s ctx inst
  (Cambrian.Generated.World.withERC20 w inst s, payload)



attribute [cambrian_route_simp] tokenId

def nonces (w : Cambrian.Generated.World) (inst : ERC20.Identity) (ctx : Cambrian.MsgCtx) (owner : Cambrian.Address) : Cambrian.Generated.World × Nat :=
  let s := w.storage.eRC20 inst
  let (s, payload) := ERC20.Local.nonces s ctx inst owner
  (Cambrian.Generated.World.withERC20 w inst s, payload)



attribute [cambrian_route_simp] nonces

end ERC20.Routes

